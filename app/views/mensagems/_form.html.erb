<div class="row">
	
<section class="col-md-offset-3 col-md-6">
	<%= simple_form_for(@mensagem, remote: true) do |f| %>
	  <%= f.error_notification %>

	  <div class="form-inputs">
	    <%= f.input :title, label: "Titulo", input_html: {class: 'form-control'} %> 
	    <%= f.input :content, label: "Conteúdo", input_html: {class: 'form-control new-mensagem-sistem'} %>
	    
	  </div>

	  <div class="form-actions">
	    <%= f.button :submit %>
	  </div>
	<% end %>
</section>
</div>